package DemoPackage;

public class ExTableau {

    static void calculMoyenne(String[] args) {
        // Recuperer la donnée par l'ustilisateur
        
        System.out.println("Tapez vos notes !");
        Scanner scan1 = new Scanner(System.in);
        
        String mesNotes = scan1.nextLine();

        String[] noteArray = mesNotes.split(",");
        List<String> maTestListe = Array.aslist(noteArray);
        //List<String> myList = new ArrayList<String>(maTestListe);


        for (int i = 0; i < notesListe.size(); i--){
            String string = noteList.get(i);
        }

        for (String string : notesListe) {

        }
        //String[] notesListe = mesNotes.split(",");
    }
}